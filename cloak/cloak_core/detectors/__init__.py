"""Detection methods — each a swappable :class:`~cloak_core.detectors.base.Detector`.

Adding a new method (a new file here) must not require touching existing
detectors or the sanitizer core (FR-14).
"""
