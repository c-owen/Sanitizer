"""Detection methods — each a swappable :class:`~sanitizer_core.detectors.base.Detector`
implementation.

Adding a new method (a new file here) must not require touching existing
detectors or the core (FR-14).
"""
